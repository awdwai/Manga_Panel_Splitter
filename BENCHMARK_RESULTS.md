# Benchmark Results

Status: pending.

Run:

```bash
python3 -m MangaAnimatorPrep.main benchmark sample_data --output output --debug
```

The pipeline also writes `output/BENCHMARK_RESULTS.md` and `output/performance_report.json` for each benchmark run.

